import { SafeAreaView } from 'react-native';
import CountrySearch from './src/screen/CountrySearch';

export default function App() {
  return (
    <SafeAreaView>
      <CountrySearch/>
    </SafeAreaView>
  );
}